{
  "name": "cjs-app",
  "version": "0.1.0",
  "description": "CJS validation app for @nuevco/free-paths",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nuevco/free-paths": "workspace:^"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^22.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  }
}